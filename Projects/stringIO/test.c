//! mission: get all char seperate by a whitespace in a line
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
    char a[100]; // declare c as an array of characters
    char b[100]; // declare c as an array of characters
    char c[100]; // declare c as an array of characters
    char d[100]; // declare c as an array of characters
    char e[100]; // declare c as an array of characters

    printf("Enter a string with space: ");
    
    scanf("%s", &a); 
    scanf("%s", &b); 
    scanf("%s", &c);    
    scanf("%s", &d); 
    scanf("%s", &e); 

    printf("output: %s\n", a); // print the string
    printf("output: %s\n", b); // print the string
    printf("output: %s\n", c); // print the string
    printf("output: %s\n", d); // print the string
    printf("output: %s\n", e); // print the string

    return 0;
}
    
